---
name: v2-competitive-roadmap
description: The active execution roadmap (docs/v2.md) for out-competing metadatakit.com, and which phase is done
metadata:
  type: project
---

The user is implementing `docs/v2.md` ("MetaDocu 竞品突破设计") step by step, in its Section E order. Strategy: don't compete on feature breadth — win on a narrower positioning (document privacy · before-sending), content competitors lack (comparison/scenario/trust/data pages), and trust + AI-citation they haven't built.

Section E landing order & gates:
1. **B — Product (DONE 2026-06-09):** made `/tools/word|excel|pdf` actually usable. Extracted the home page's scan/edit/batch workbench into a reusable, type-scoped client component `components/om/om-workbench.tsx` (`scope` prop: all/word/excel/pdf restricts the file picker + format badges). Home page and all three tool pages now consume it; coming-soon placeholders removed. De-Chinese'd `om-audit-report.tsx` (its 3D-flip green "100% CLEAN" back-card IS the post-clean verification report the plan wanted). Added `workbench` i18n namespace + new `audit` keys to both locales. Tool UI confirmed ad-free; `/editor` + `/batch` already complete the loop. typecheck/seo:check/build green.
2. **A + D1 + D5 (DONE 2026-06-09, except user-only actions):** Standardized the brand entity to **"MetaDocu"** everywhere — fixed `APP_NAME` (was `packageJson.name` = lowercase `metadocu`) to `SITE_NAME`, fixed `generate-json-ld.ts` `SITE_NAME` (was Chinese `Office元数据编辑器`), and `layoutJsonLd*` message keys (were `MetaDocu Privacy Scanner`) → all "MetaDocu". Shifted hero/meta/OG copy to the **before-sharing positioning** ("Clean Hidden Data From Your Documents Before You Share" + named audiences) in `common.appTitle`/`home.description`/`layout*` + home & about seo-map entries, both locales. Enriched the global Organization JSON-LD (logo + contactPoint email) in root `layout.tsx`; **no `sameAs`** (user has no public profiles yet). Removed duplicate Org/WebSite from home seo-map (now global-only). Added missing `app/[locale]/about/layout.tsx` (about had NO TDK — it's a client component, couldn't export generateMetadata) + AboutPage/Breadcrumb JSON-LD. Made breadcrumb home label locale-aware (was hardcoded `首页`). D5: home/about `internalLinksTo` now point to tool hubs; footer already links all hubs site-wide; sitemap auto-covers all published pages.
   - **STILL USER-ONLY (not done):** submit sitemap to GSC + Bing; create Wikidata entry for entity disambiguation; provide real GitHub/X/LinkedIn URLs so `sameAs` can be added (search shows MetaDocu confused with Metadoc/MetaDoc.AI — sameAs+Wikidata are the fix).
3. **D3 + C3 (DONE 2026-06-09); C1/C2 still pending.** Built the D3 "unique data base" as `lib/seo-data/metadata-exposure.ts` — a bilingual (en+zh) structured dataset of 16 rows (format × hidden field × what it exposes × risk × how MetaDocu removes it), with `exposureFieldsForFormat()`/`exposureCountForFormat()` helpers. Rendered via reusable `components/om/om-metadata-exposure-table.tsx` (locale-aware, optional per-format filter). Built C3 flagship trust page at **`/is-it-safe`** (`app/[locale]/is-it-safe/{layout,page}.tsx`, server component): answer-first lead block (GEO citability), 4-step how-in-browser-works, local-vs-upload comparison table, the D3 exposure table, CTA to tool hubs, and FAQ via SeoContent. Added seo-map entry `trust.is-it-safe` (level 2, FAQPage+BreadcrumbList, 5 PAA) + the full 4-file FAQ wiring (faq-keys both langs + seo.faqQuestions/faqAnswers both locales, each answer 100–150 words answer-first) + `trustSafe` message namespace both locales. D5: wired the trust page into footer (`linkSafe`), all 3 tool pages' `internalLinksTo` (RelatedLinks), home `internalLinksTo` + `quickLinks`. Also fixed a pre-existing site-wide leak: `RelatedLinks.tsx` used non-localized `primaryKeyword` as anchor (Chinese leaked onto EN pages) → now uses `en.h1` on EN locale.
   - **First C1 comparison page DONE 2026-06-09:** `/metadatakit-alternative` (`app/[locale]/metadatakit-alternative/{layout,page}.tsx`) + seo-map `compare.metadatakit` + `compareKit` messages (both locales) + 4 PAA wired + linked from home quicklinks/internalLinksTo and the trust page. **Analyzed metadatakit.com directly (WebFetch):** it is ALSO 100% local/WebAssembly/no-upload, free, AND ad-free — so "no upload" and "ad-free" are NOT differentiators vs it (do NOT claim otherwise — false + Google-trust risk). Its real strengths: 500+ formats, photos/RAW/audio(ID3)/video, shutter-count, GB files, side-by-side compare; it's faceless (no About). MetaDocu's honest edge: document-privacy focus, before-sharing workflow, post-clean verification report, scenario/education content, entity/E-E-A-T. The page concedes their strengths openly (honest comparison, "competitor details as described on their site") — this is intentional and required (v2.md §63: never thin截流 shells).
   - **USER SHOULD VERIFY before deploy:** the competitor facts on the comparison page (formats/no-upload/ad-free claims) are from metadatakit.com's own site as of 2026-06; re-confirm before publishing since it names a competitor.
   - **NEXT (step 3 remainder):** more C1 pages (iLovePDF / Smallpdf / "without Adobe Acrobat" alternatives) and C2 scenario/profession pages (resume/contract/legal-PDF/GPS-photo/RSID) — generate C2 from the D3 dataset so each is unique.
   - **GOTCHA:** message JSON and the D3 `.ts` use real ASCII strings — do NOT put ASCII double-quotes inside zh strings (breaks JSON/TS parse). Use 「」 brackets for inner quoting (hit this during the build).
4. C4 (original data study — highest-leverage backlink/citation asset).
5. D4 (monthly AI-citation tracking vs competitors).

Notes: docs/v2.md references "《执行方案 v4》" and "《审查与补强》" that DON'T exist as files (only docs/v1.md, v2.md) — those cross-refs are dead; follow v2.md's own checklists. Repo has lots of Chinese in source (comments OK, but hardcoded user-facing strings are a known leak the plan wants cleaned as we touch files). `pnpm lint` was already red on main (pre-existing `any`/ts-ignore errors in wasm-worker.ts, useWasmWorker.ts, om-metadata-edit-view.tsx) — not introduced by this work.
